{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Example creation</h1>

    {{ form(form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('example') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
